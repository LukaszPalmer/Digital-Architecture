export interface PricingPackage {
    id: string;
    name: string;
    label: string;
    description: string;
    features: string[];
    cta: string;
    highlighted?: boolean;
    paket: string;
}